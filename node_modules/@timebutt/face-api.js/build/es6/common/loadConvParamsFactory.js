export function loadConvParamsFactory(extractWeightEntry) {
    return function (prefix) {
        var filters = extractWeightEntry("".concat(prefix, "/filters"), 4);
        var bias = extractWeightEntry("".concat(prefix, "/bias"), 1);
        return { filters: filters, bias: bias };
    };
}
//# sourceMappingURL=loadConvParamsFactory.js.map